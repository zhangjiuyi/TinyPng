{
  "name": "compressimages",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "window": "rm -rf ./build && electron-packager . tinyPng --out=./build --platform=win32 --icon=./icons/app.ico",
    "mac": "rm -rf ./build && electron-packager . tinyPng  --out=./build --platform=darwin --icon=./icons/app.icns",
    "linux": "rm -rf ./build && electron-packager . tinyPng  --out=./build --platform=linux",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "JiuyiZhang",
  "license": "ISC",
  "devDependencies": {},
  "dependencies": {
    "electron": "^1.7.12",
    "tinify": "^1.5.0"
  }
}
